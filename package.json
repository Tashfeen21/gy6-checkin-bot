{
  "name": "gy6-checkin-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Custom Slack check-in/check-out attendance bot for GY6 (Cloudflare Workers + Notion).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
